# Run 260618 — M4 Pro, Chrome stable

Both runs executed inside the offscreen document. `navigator.gpu present: true — adapter: apple metal-3`.

## webgpu + fp32
- Model load (cold, first run incl. ~326MB fp32 download + shader compile): **26.2s**
- short: 3.90s audio / 2.68s gen → **1.45× RT**
- long:  26.55s audio / 2.79s gen → **9.52× RT**
- Note: short gen (2.68s) ≈ long gen (2.79s) despite 7× more audio → cost is dominated by a
  one-time warmup/first-call overhead (~2.7s), absorbed by whichever call runs first (short here).
  Steady-state marginal cost per token on GPU is very small; long RTF ≈ true throughput.

## wasm + q8
- Model load (q8 ~86MB, cached, no GPU compile): **0.4s**
- short: 3.90s audio / 5.46s gen → **0.71× RT**
- long:  26.50s audio / 37.12s gen → **0.71× RT**
- Single-threaded WASM blocks the offscreen doc thread for the whole 37s → Chrome "page
  unresponsive" dialog. The console "VerifyEachNodeIsAssignedToAnEp" line is a benign ORT
  **warning** (shape ops on CPU), not the cause; audio still produced.

## Verdict inputs
- Gate: PASS (real Metal-3 adapter, inference ran in offscreen).
- Perf: webgpu 9.52× RT = ~2.07× Docker's 4.6×. wasm 0.71× RT = 6.4× slower than Docker.
- Correctness: clip plays as intelligible af_heart speech (user confirmed run; explicit audio-quality
  sign-off pending).
