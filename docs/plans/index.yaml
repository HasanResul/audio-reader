docs:
  260617_audio-reader-mvp.md: Brave extension + local OpenAI-compatible TTS server; phased Kokoro MVP (status: active; Phase 0 done — Docker CPU Kokoro proven, ~0.8s first-audio, 4–5x RTF; Phase 1 done — speak-selection MVP in extension/, user-verified in Brave; Phase 2 whole-article extraction remaining)
  260618_browser-tts-engine.md: add in-browser Kokoro engine (WebGPU/WASM) alongside Docker; one extension, availability-gated engine picker + status icon (status: draft)
