subdirs:
  tts-models: TTS model landscape research for Apple Silicon narration
  tts-tooling: tooling landscape — extensions, article extraction, overlay players, local TTS servers
  chrome-ext-webgpu: Chrome MV3 extension WebGPU integration — offscreen docs, kokoro-js, CSP
